fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0

# LangGraph + LangChain
langgraph>=0.2.0
langchain>=0.2.0
langchain-anthropic>=0.1.15
langchain-core>=0.2.0

# Anthropic SDK
anthropic>=0.28.0

# Vector DB
qdrant-client>=1.9.0

# Graph DB
neo4j>=5.20.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1  # passlib 1.7.4 is incompatible with bcrypt 4.1+
python-multipart>=0.0.9

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.19.0      # SQLite (local dev)
asyncpg>=0.29.0        # PostgreSQL (production)

# HTTP
httpx>=0.27.0

# Utilities
python-dotenv>=1.0.0
eval_type_backport>=0.1.3  # Python 3.9 compatibility for X | Y union syntax
greenlet>=3.0.0  # Required by SQLAlchemy async engine
